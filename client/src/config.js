const config = {
  clientUrl: process.env.TASKKA_CLIENT_URL,
  serverUrl: process.env.TASKKA_SERVER_URL,

  google: {
    clientId: process.env.TASKKA_GOOGLE_CLIENT_ID
  },
};

export default config;
